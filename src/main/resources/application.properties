#DB properties:
spring.datasource.url=jdbc:mysql://localhost:3306/cloudusers
spring.datasource.username=veryval
spring.datasource.password=12398755
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.sql.init.mode=always
spring.sql.init.data-locations=classpath:data.sql

#CORS
server.port=5500
allowed.uri.localhost=http://localhost:8080

#Hibernate Configuration:
spring.jpa.hibernate.show_sql=true
spring.jpa.hibernate.ddl-auto = none

#JWT
jwt_secret=mySecretKey

#LOG
logging.file.name=src/main/resources/log_server.log
logging.pattern.file= "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
